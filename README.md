# ChatRSA

```bash
 git clone
 cd ChatRSA
 pip install -r requirements.txt
 flask db init
 flask db migrate -m "message"
 flask db upgrade
 python .\server\server.py
 python .\client\client.py (for every chat instance)
```
